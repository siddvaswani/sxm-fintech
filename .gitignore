# Tool-local config (per Sidd's convention — never commit)
.claude/
.superpowers/

# Secrets — env files + private keys (NEVER commit real values)
.env
.env.*
!.env.example
*.key
*.pem

# Dependencies
/node_modules
/.pnp
.pnp.*

# Next.js / build output
/.next/
/out/
/build
dist/
*.tsbuildinfo
next-env.d.ts

# Testing
/coverage

# Vercel
.vercel

# OS / debug logs
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
